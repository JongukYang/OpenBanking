// var x = function (x, y) {
//   return x * y;
// };

// const x = (x, y) => x * y;

function plus(p1, p2) {
  console.log(p1 + p2);
}

const plusArrowFunc = (p1, p2) => {
  console.log(p1 + p2);
};

plus(5, 3);
plusArrowFunc(2, 4);
